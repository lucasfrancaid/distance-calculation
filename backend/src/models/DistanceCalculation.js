module.exports = DistanceCalculation = {
    distance: String,
    duration: String,
    origins: {
        lat: String,
        lng: String
    },
    destinations: {
        lat: String,
        lng: String
    },
    zoom: Number,
    center: {
        lat: String,
        lng: String
    }
}